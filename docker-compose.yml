version: "3.9"

services:
  api:
    build: .
    ports:
      - "10000:10000"
    env_file:
      - .env
    volumes:
      - ./app:/app/app
    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 10000
      --reload
      --log-level debug
    restart: unless-stopped
